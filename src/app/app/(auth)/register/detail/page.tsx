import RegisterInfo, { metaDataRegisterInfo } from "@/services/auth/pages/register_info";

export const metadata = metaDataRegisterInfo;
export default function Page() {
    return (
        <RegisterInfo />
    )
}